.devise-registration
  .devise-registration__header
  .devise-registration__main
    .devise-registration__main__top 会員情報変更
    = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
      = render "devise/shared/error_messages", resource: resource
      .field
        %label.field--title アーティストネーム
        %span.field--span 必須
        %br/
        = f.text_field :artistname
      .field
        %label.field--title メールアドレス
        %span.field--span 必須
        %br/
        = f.email_field :email, autofocus: true, autocomplete: "email"
      .field
        %label.field--title パスワード
        %span.field--span 必須
        %br/
        = f.password_field :current_password, autocomplete: "current-password"
      .field
        %label.field--title プロフィール
        %br/
        = f.text_area :profile
      .field
        %label.field--title アーティスト写真
        %br/
        = f.file_field :artistimage
      .actions
        = f.submit "Update"
    = link_to "Back", :back
