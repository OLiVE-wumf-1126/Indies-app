.index__top{ style: "background-image: url('/assets/top-image.jpg')"}
  .index__top--text
    全国のリスナーへ
    %br
    あなたのライブをお届けします
.index-content
  = render "index-content_center"
  .index-content__right
    -if listener_signed_in?
      = render "listenershow/show"
    - else
      = render "right-content_signedin"
.index-paginate
  = paginate(@posts)
